/**
 * Top-level re-exports for the most common helpers.
 * Deep imports (e.g. `@/lib/format/numbers`) still work — this is
 * a convenience surface only.
 */
export { cn } from "./utils";
export * from "./initials";
export * from "./constants";
