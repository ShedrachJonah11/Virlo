/**
 * Re-export of `cn` — the project's class-name combiner.
 * `import { cn } from "@/lib/cn"` reads slightly nicer in JSX
 * than `from "@/lib/utils"` and survives any future refactors
 * of `lib/utils.ts`.
 */
export { cn } from "./utils";
